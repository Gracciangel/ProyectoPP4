export type TableResponse = {
  success: boolean;
  message: string;
};

export type EmailResponse = {
  exist: boolean;
  message: string;
};

export type CreateUserResponse = {
  success: boolean;
  message: string;
};
